plugins {
    id 'java'
    id 'jacoco'
}

version = '1.0.0'

dependencies {
    testCompile "junit:junit:4.12"
}

repositories {
    jcenter()
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}
